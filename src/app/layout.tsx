import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Low-Voltage Estimator | Estimador de Costos de Bajo Voltaje",
  description: "Plataforma de cotización rápida para proyectos de sistemas especiales de bajo voltaje: CCTV, Control de Acceso, Voceo y Detección de Incendios.",
  keywords: ["estimador", "bajo voltaje", "CCTV", "seguridad", "cotización", "presupuesto"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}